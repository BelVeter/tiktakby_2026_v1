<?php

return [
    /*
    |--------------------------------------------------------------------------
    | MCP API Token
    |--------------------------------------------------------------------------
    | Статичный Bearer-токен для доступа к MCP API.
    | Генерация: openssl rand -hex 32
    */
    'api_token' => env('MCP_API_TOKEN', ''),

    /*
    |--------------------------------------------------------------------------
    | Geo Country Restriction
    |--------------------------------------------------------------------------
    | Список разрешённых кодов стран ISO 3166-1 alpha-2, через запятую.
    | Например: MCP_GEO_ALLOWED_COUNTRIES=BY,RU
    | Если пусто — ограничение по стране отключено (режим разработки).
    */
    'geo_allowed_countries' => array_filter(
        array_map('trim', explode(',', env('MCP_GEO_ALLOWED_COUNTRIES', '')))
    ),

    /*
    |--------------------------------------------------------------------------
    | Cache TTL (seconds)
    |--------------------------------------------------------------------------
    */
    'cache_ttl' => (int) env('MCP_CACHE_TTL', 300), // 5 минут

    /*
    |--------------------------------------------------------------------------
    | API Version
    |--------------------------------------------------------------------------
    */
    'version' => '2.5.0',
];
