# SEO-аудит 404 (24.09.2026): что нашли и что сделали

Контекст: другой ИИ-агент (проект SEO) выгрузил 876 URL, которые Яндекс получил как 404 с 24.08.2026
(файл `404-audit-2026-09-23.csv` остаётся у него, в этот репозиторий не копируется) и предложил пять
задач. Мы перепроверили его выводы по коду, логам и БД прода (только чтение) и часть исправили.
Ответ агенту SEO-проекта с позицией и полным отчётом — [reply_to_seo_agent_404_audit_2026-09-24.md](reply_to_seo_agent_404_audit_2026-09-24.md). Открытые пункты — в [backlog.md](backlog.md), раздел «SEO-аудит 404»; шаги выкатки — в
[prod_pending.md](prod_pending.md). Логи прода: `/var/www/h149208/data/logs/` (хранятся с 13.09).

## Что оказалось на самом деле

| Тема | Вывод |
|---|---|
| «Мигающие» 404 у карточек из sitemap | Не мигание на часы. У модели нет строк в `tovar_rent_items` (последнюю единицу продали/списали), `L3Controller` отдаёт 404 (`l3_not_found`, PR #144). Слинг 939 — 30 дней (22.08 → 21.09), весы 1326 — ~88 дней (26.06 → 22.09). Автосинхронизации нет: `DELETE FROM tovar_rent_items` есть только в ручных операциях админки. 404 — сознательное поведение, а не баг; нужна политика (см. backlog). |
| 404 «в sitemap» | Генератор и L3 используют одно условие (есть единицы), рассинхрон только суточный. Реальная причина — `sitemap.xml` лежит в git, а `Deploy.php` делает `git reset --hard`: 23.09 в 23:51 вернулась версия от 11.08 (70 из 1021 адреса не 200). |
| Иконки | `/tiktak.ico` и `/public/favicon-32x32.png` отдавали 404 на каждой странице (по ≈6 тыс. строк лога за 10 дней; лог дублирует строки). `/favicon.ico` на проде был пустым файлом в 0 байт. `.gitignore` игнорирует `*.png` и `*.ico`. |
| Мусорные префиксы (`/kovriki/…`, `/httpss:/…`) | Общий механизм уже есть: `RedirectController::notFound` ищет по последнему сегменту и даёт 301 (≈6.2 тыс. строк 301 против ≈2.2 тыс. 404 за 10 дней). Баг: он строит адрес через M:N `subrazdel_category`, а 18 из 111 категорий там нет. Трафик мусорных URL — боты без referrer. |
| 470 «мёртвых» товаров | 414 слагов нет в каталоге; 53 — модели `show` без единиц (тот же механизм, что у «мигающих»); 1 категория; 2 восстановленных. |
| Категория `tyu` | Оказалась служебной свалкой «К УДАЛЕНИЮ» (id 175) с 10 опубликованными моделями — см. [db_notes.md](db_notes.md), п. 20–22. |

## Что сделано

| Что | Где | Статус |
|---|---|---|
| Иконки: набор из знака-цветка (ico 16/32/48, png 32/120/192, apple 180), исключения в `.gitignore`, `/delivery` → `/ru/delivery`, тест `LayoutIconLinksTest` | PR #322 | влит, выкачен и проверен 24.09 |
| Фолбэк редиректов ищет родителей по канонической цепочке (`main_sub_razdel_id` → `sub_razdel.main_razdel_id`). Пробный прогон на прод-данных (611 слагов, только SELECT): 21 адрес 404→301, у 7 цель меняется на канонический, потерь нет | ветка `fix/redirect-fallback-canonical-chain` | влит, выкачен и проверен 24.09 |
| Категория 175 скрыта: рекомендации, поиск, фильтры, sitemap (генератор и закоммиченные файлы), цена/фото 2ГИС-фида; карточки и страница категории → 301 на `/ru/karnavalnye-kostyumy` (Known Specifics #17 в `AGENTS.md`) | PR #324 | влит, выкачен и проверен 24.09 |
| Удалены 10 редиректов `/karnaval/<slug>.htm → …/tyu/<slug>` | прод, через MCP API, 24.09 | выполнено |
| Значок «Важно!» (красный круг с «!», `public/images/vazhno.png`) | файл загружен на прод вручную 25.09; ветка `feat/vazhno-icon` | файл отдаётся 200; ветка ждёт слияния, чтобы файл отслеживался git |
| Контент: 15 карточек через `PATCH /pages/product/bulk`, главная и категория колясок (`pages` id 1, 22) и редирект id 701 | прод, MCP API и точечный UPDATE, 25.09 | выполнено, откат — `storage/app/backups/seo-content-2026-09-25/` и `/pages/history` |

## Проверка после выкатки (24.09.2026, прод на `916a91f`)

- Страница категории и все 10 карточек → 301 на `/ru/karnavalnye-kostyumy`, в том числе на чужом префиксе.
- sitemap: 1010 адресов, ни одного с `/tyu`. На страницах RF-лифтинга и Дарсонваля, в поиске «Колобок» и в
  фильтре по производителю «Батик» ссылок на `/tyu/` нет.
- Фолбэк: `…/sidenie_dlia_samokata_micro`, `/kovriki/slingi`, `…/kacheli-napolnye`,
  `…/hodunki_chicco_band_green_wave` → 301 на канонические адреса (раньше 404); неизвестный слаг → 404;
  `/prokat/prokat-detskih-tovarov/kolyaski-detskie` → 301 как и прежде.
- Иконки отдаются 200 (`/favicon.ico` 5646 байт), `/tiktak.ico` по-прежнему 404, но на него никто не ссылается.
- В логах после выкатки нет 5xx и PHP-ошибок.
- Цена секции «физиотерапия» в 2ГИС-фиде осталась 13.50: фид перегенерирован в 18:24, все 22 предложения
  совпали с прежними. Правка фида ничего не изменила (скрытые костюмы цену не искажали).
- Известное замечание: мусорный префикс + слаг скрытой модели → два 301 подряд (см. backlog).

## План работ (актуально на 25.09.2026)

Единый план по аудиту. Подробности и «почему» — в [reply_to_seo_agent_404_audit_2026-09-24.md](reply_to_seo_agent_404_audit_2026-09-24.md),
техдолг по пунктам — в [backlog.md](backlog.md). Решение владельца от 25.09: все находки (блок Б) в плане,
занимаемся ими **в конце**, после блока А.

### А. Основные пункты

| # | Что | Статус |
|---|---|---|
| 1 | Иконки, `/delivery` → `/ru/delivery` | выполнено, PR #322, выкачено |
| 2 | Фолбэк редиректов по канонической цепочке | выполнено, выкачено 24.09 |
| 2б | Категория «К УДАЛЕНИЮ» (id 175) скрыта: рекомендации, поиск, фильтры, sitemap, 2ГИС-фид, 301 на карточки и страницу | выполнено, PR #324, выкачено |
| 3 | Контент: 15 карточек, главная, категория колясок, редирект id 701; значок «Важно!» | выполнено 25.09. Файл значка загружен на прод вручную по поручению владельца (SFTP, SHA-256 совпал, 200 `image/png`); PR `feat/vazhno-icon` нужен, чтобы файл и исключение `.gitignore` попали в git |
| 4 | Генератор sitemap: исключить категорию Биоптрона (301), слаги с пробелом и `&amp;`, убрать дубли URL по M:N (один канонический адрес на категорию и модель) | ждёт |
| 5 | `sitemap.xml` вне git: оба файла в `.gitignore`, тест `SitemapNotTrackedTest`. `Deploy.php` не менялся (решение владельца 25.09): свежий файл создаёт ночная генерация (02:00), первую выкатку добираем ручным `sitemap:generate` | ветка `fix/sitemap-out-of-git`, ждёт PR и деплоя |
| 6 | Политика для модели без единиц: 404 или 200 «нет в наличии» | ждёт решения владельца |
| 7 | Таблица «URL → 301 или 404» для 470 мёртвых URL и аудит целей всех 765 редиректов | после п. 6 |

### Б. Находки (займёмся в конце)

| # | Находка | Что делать | Где подробнее |
|---|---|---|---|
| Б1 | **Soft-404:** любая выдуманная категория под настоящим разделом отдаёт 200 «Раздел не найден.» (`MainPage::isRealPage()`) | код: 404, если категория не найдена | backlog |
| Б2 | **Потерянные картинки** (`bb/img`, `karnaval/img`, `vesy/img`, папка Maxi-Cosi): ≈35 карточек и `pages` id 42, 45, 50, 67, около 300 запросов за 10 дней | решение: восстановить из бэкапа старого хостинга или убрать теги `<img>` через API | reply, раздел 5.2; backlog |
| Б3 | **Судьба 10 моделей «К УДАЛЕНИЮ»** и ~50 исторических моделей без страниц + 3 учётные позиции (622, 900, 1404); 11 заявок за год | перенос в карнавальные категории, если вещи есть, либо списание единиц и архивация (`ModelArchive`) | db_notes п. 20 |
| Б4 | **Сиротский и недостижимый контент `pages`:** id 22 (`detskie-kolyaski`, 200 «Раздел не найден.»), id 25 (за редиректом, относительные ссылки); бренды Maclaren и Quinny без живых моделей в тексте категории колясок | решить судьбу страниц и текста | backlog |
| Б5 | **18 категорий вне `subrazdel_category`** (не видны в меню): `tolshchinomery` (2 живых товара), `aksessuary-dla-samokatov` (1) | выяснить, намеренно ли скрыты | backlog |
| Б6 | **Два 301 подряд:** мусорный префикс + слаг скрытой модели → `…/tyu/<slug>` → карнавал | отдавать итоговый адрес за один шаг | backlog |
| Б7 | **Слаги, не соответствующие товару:** `novogodniy_kostume_cherny_plasch` (Бэтмен), `sukenka-belarus` (куб); API слаги не меняет | по желанию, со 301 | backlog |
| Б8 | **Каноническая цепочка против M:N:** дубли URL в sitemap (`/prokat-detskih-tovarov/` и `/prokat-sports/` для одной категории), 14% моделей с неканоническим адресом в старом API | решается вместе с п. 4 | reply, раздел 5.5 |
| Б9 | **`not_show` не закрывает прямой адрес** (`ModelWeb::getByUrlName()` игнорирует статус) | только документация: закрывает архивация или скрытая категория | db_notes п. 22 |
| Б10 | **`.floatLeft` не определён в CSS**; правка значка сделана inline-стилем | по желанию завести класс в CSS | db_notes п. 23 |

Предлагаемый порядок «в конце»: Б1 (маленькая правка кода, убирает целый класс мусорных 200) → решение по Б2 →
пункты 4 и 5 → решение по п. 6 → Б3–Б5, Б6–Б8.

## Удалённые редиректы (для отката)

Все — `301`, активные, не regex, комментарий «Legacy HTML cleanup» (пакетная загрузка карты старых
`.htm`-адресов через API; автоматики, которая их создаёт, в приложении нет — в `redirects` пишут только
`RedirectsController` и две миграции слияния моделей). Источник: `/karnaval/<slug>.htm`, цель:
`/ru/medical-prokat/electroterapiya-prokat/tyu/<slug>`.

| id | slug | переходов за всё время |
|---|---|---|
| 71 | `costume_dlya_malchika_sobaka` | 13 |
| 92 | `costume_masha_i_medved` | 11 |
| 182 | `karnavalnyi_costume_myshki` | 5 |
| 183 | `karnavalnyi_costume_petushok` | 1 |
| 187 | `karnavalnyi_kostum_dakonchik_dazi` | 4 |
| 217 | `kolobok_kostum_novogodnii` | 6 |
| 224 | `kostjum_porosjonka_na_novogodnij_utrennik` | 5 |
| 237 | `kostum_dlya_devochki_tsarevna_lyagushka` | 0 |
| 337 | `novogodniy_kostume_cherny_plasch` | 8 |
| 343 | `novogodnyi_costume_korol` | 6 |

Восстановить: `POST /api/mcp/v1/redirects/bulk` с телом
`{"redirects":[{"source_url":"/karnaval/<slug>.htm","target_url":"/ru/medical-prokat/electroterapiya-prokat/tyu/<slug>","status_code":301,"comment":"Legacy HTML cleanup"}, …]}`
(после включения скрытия категории 175 такая цель всё равно уйдёт в 301 на `/ru/karnavalnye-kostyumy`).

## Как перепроверить

```bash
# 404 на иконки в логе прода (после выкатки #322 новых быть не должно)
zgrep -h -E 'GET /(tiktak\.ico|public/favicon-32x32\.png)' /var/www/h149208/data/logs/tiktak.by.access.log | tail -3
# адреса из sitemap (после выкатки fix/sitemap-out-of-git деплой файл не откатывает; до неё сверять только после 02:00)
curl -s https://tiktak.by/sitemap.xml | grep -o '<loc>[^<]*' | sed 's/<loc>//' | head
# скрытая категория и карточка (любой префикс) → 301 на /ru/karnavalnye-kostyumy
curl -sI https://tiktak.by/ru/medical-prokat/electroterapiya-prokat/tyu | head -3
curl -sI https://tiktak.by/ru/prokat-detskih-tovarov/x/y/kolobok_kostum_novogodnii | head -3
# sitemap без скрытой категории — ждём 0
curl -s https://tiktak.by/sitemap.xml | grep -c 'electroterapiya-prokat/tyu'
# какие редиректы целятся в скрытую категорию
curl -sH "Authorization: Bearer $TIKTAK_MCP_TOKEN" 'https://tiktak.by/api/mcp/v1/redirects?search=electroterapiya-prokat/tyu&per_page=50'
```
