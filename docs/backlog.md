# Backlog (доработки и техдолг)

Список отложенных задач и техдолга. Заводится по ходу работы. Связанные находки — в [docs/db_notes.md](db_notes.md).

## Легаси для чистки (мёртвый код, найдено 2026-06-05)

Подтверждено grep'ом — нигде не подключается / уже несовместимо со схемой:

- [x] `bb/bron.php` — глобальный `class bron` без namespace; живой класс — `bb\classes\bron`. Никто не require'ил. **Удалён 27.08.2026.** При удалении вскрылось, что на этот несуществующий глобальный класс всё же ссылаются три места — см. следующий пункт.
- [ ] **Починить обращения к несуществующему глобальному `bron`** (найдено 27.08.2026, ломается независимо от удаления файла — глобального `class bron` в рантайме не было никогда): `bb/scanner_tovar.php:887` `\bron::getFirstBronForInv()` — ветка `$tov->status == 'bron'` на живой «Карточке товара»; `bb/rent_orders_arch.php:372` `new bron()` в ветке `action='сохранить'`; `bb/dogovor_new2.php:1462` `new bron()` во вставке на стирку после возврата. Фикс: `use bb\classes\bron;` в шапку файла (для `scanner_tovar.php` — заменить `\bron::` на `bron::`, там уже `namespace bb;`).
- [ ] `bb/l_3_br.php` — старая схема INSERT; шаблоны постят на `includes/l_3_br.php` (живой).
- [ ] `bb/classes/old_bron.php` — старая схема (меньше колонок), не подключается.
- [ ] `bb/bron/rent_orders.php` — старая схема, не подключается.
- [ ] `includes/zvonok.php` — позиционный INSERT в `zvonki` (11 значений vs 14 колонок) → уже несовместим; живой путь — `ZvonokController`. Статические `.html`-хедеры, которые на него постят, переадресованы.
- [ ] `includes/*.html` — старые статические хедеры (`header.html`, `header_karnaval.html`, `header_igrushki.html`, `arch/*`). Все переадресованы; кандидаты на удаление.
- [ ] Прочие `* copy.php` (`bb/scanner_tovar copy.php`, `bb/top_menu copy.php`) — проверить и удалить.

## Известные пре-существующие проблемы

- [ ] `tests/Feature/Mcp/SpecRuntimeParityTest::spec version matches` падает на `main`: тест ждёт версию OpenAPI `2.0.1`, а спека уже `2.2.0`. Не связано с заявками (область MCP/rocketsms). Фикс — синхронизировать assertion с актуальной версией спеки (обновлять оба вместе).

## SEO-данные каталога (найдено 2026-07-23 при доработке `/pages/product`)

- [ ] **Пустой `<title>` на L3.** `resources/views/layouts/app.blade.php`: `<title>@yield('page-title')</title>` без фолбэка, у 18 моделей `rent_model_web.title` пустой → страница отдаёт `<title></title>`. На L2 фолбэк генерируется. Мелкая правка: дефолт вида «Прокат {name} в Минске».
- [ ] **`@yield('meta-description')` не экранируется** — кавычка или тег в поле ломают `<meta>`. Со стороны API это закрыто санитайзером в `PagesProductController`, но админка `bb/model_web.php` вырезает только кавычки, а L2-эндпоинт не чистит ничего. Полное решение — экранировать в шаблоне (сейчас в БД таких значений нет, двойного экранирования сущностей не будет).
- [ ] **6 групп дублей `page_addr`** в `rent_model_web` (одна из них — два разных `model_id`). API теперь отвечает 409, но данные надо развести в админке.
- [ ] **~117 моделей `status='show'` без канонического URL** — категория не привязана ни к одному `main_sub_razdel_id`. Страниц у них нет, в sitemap не попадают; кандидаты на `status='not_show'`.
- [ ] **26 групп моделей с одинаковым `item_name_main`** = дубли H1 на живых страницах. Поле теперь пишется через `PATCH /pages/product/{slug}` (`h1`), но правки видны и в печатных формах `bb/l_3_br.php` — проверять контекст.

## Мёртвая форма прямого заказа на L3 (найдено 2026-08-12)

Заказ с сайта оформляется только через корзину, но в карточке товара остался
целый путь заказа с тех пор, как корзины не было. Из интерфейса он недостижим —
проверять надо вызовы, а не наличие разметки:

- [ ] `resources/views/includes/l3_tovar_info_block.blade.php`: функция `l3CheckCartBeforeOrder()` объявлена, но **нигде не вызывается**, вместе с ней мертва модалка `cartWarningModal` («у вас есть товары в корзине — заказать только этот?»).
- [ ] Модалку `#orderModal` открывает единственная кнопка «Оставить заявку», и она рендерится только в ветке `@else` — когда товара **нет** в наличии. Поля доставки в форме скрыты атрибутом `data-show="whenfree"`.
- [ ] Следствие: ветка «есть свободный товар» в `L3Controller::l3Order2` (создание брони через `createBronStrong`) практически недостижима — попасть в неё можно лишь гонкой, когда товар освободился, пока клиент заполнял форму заявки, и тогда доставка всё равно не отмечена.
- [ ] Чистить эти три куска стоит одним заходом и только по явному запросу: форма может понадобиться, если прямой заказ решат вернуть.

## SEO-аудит 404 (2026-09-24)

Проверка промпта другого ИИ-агента по логам и БД прода. Выводы, факты и список сделанного —
[seo_404_audit_2026-09-24.md](seo_404_audit_2026-09-24.md). Выгрузка аудита (876 URL) остаётся у агента SEO-проекта.

- [x] **`sitemap.xml` лежал в git, а `Deploy.php` делает `git reset --hard`** — исправлено (PR #327,
  выкачено 25.09). Каждый деплой откатывал свежий файл к закоммиченной версии до генерации в 02:00
  (23.09 в 23:51 вернулась версия от 11.08: 70 из 1021 адреса не отдавали 200; 25.09 в 10:10 снова).
  Теперь оба файла в `.gitignore` и вне git (тест `SitemapNotTrackedTest`). `Deploy.php` не менялся (решение
  владельца); сам деплой один раз удалил файлы с сервера, они пересозданы вручную `php artisan sitemap:generate`.
- [x] **Генератор sitemap выводил дубли, редиректящую категорию и невалидный адрес** — исправлено веткой
  `fix/sitemap-generator-canonical` (тест `SitemapGeneratorTest`): адрес строится по канонической цепочке
  `sub_razdel.main_razdel_id`, а не по M:N `razdel_subrazdel` (на проде уходит ≈51 дубль подраздела
  `begovely_velosipedy_samokaty` под `prokat-sports`: 1 подраздел, 7 категорий, 43 модели); категория
  `/ru/medical-prokat/bioptron-prokat-minsk/prokat-bioptron-minsk` (301 из `routes/web.php`) исключена, алиас
  `/ru/medical-prokat/bioptron` остаётся; сегменты адреса кодируются (`%20`, `%26`). `--verify` «на копии» не
  годится: `BASE_URL` зашит на прод.
- [x] **Внутренние ссылки на редиректящую категорию Биоптрона (Б13)** — исправлено веткой
  `fix/sitemap-generator-canonical`. Меню, список категорий, хлебные крошки карточек и редирект `/ru/prokat/{cat}`
  вели на `/ru/medical-prokat/bioptron-prokat-minsk/prokat-bioptron-minsk` (301 → `/ru/medical-prokat/bioptron`),
  на главной — сразу на алиас. Теперь `Category::getUrlForPage()` отдаёт алиас из `Category::URL_ALIASES`
  (тест `CategoryUrlAliasTest`).
- [ ] **Slug с пробелом и `&` (модели 1465 `pelenalnyj_stolik _s_vannochkoj_cam_cambio` и 558
  `laugh_&_learn_smart_stages_home`).** В sitemap они теперь закодированы, но `<link rel="canonical">` страниц
  содержит их в сыром виде. Правильно переименовать `rent_model_web.page_addr` и поставить 301 со старых
  адресов (запись в БД — с согласия владельца; API `page_addr` не меняет; проверить, что `CheckRedirects` ловит
  адрес с пробелом).
- [ ] **Разовый 500 на карточке после деплоя (Б11).** 25.09 15:44:40 при обходе sitemap в 6 потоков:
  `file_put_contents(storage/framework/cache/data/…)`: No such file or directory в
  `Cache::remember('rec_view_ids_…')` (`app/MyClasses/L3Page.php:333`) — гонка файлового кэша после
  `cache:clear` при деплое. Повтор — 200, в текущем логе единственная такая ошибка. Если повторится:
  прогрев кэша после деплоя или другой драйвер кэша для рекомендаций.
- [ ] **Модель без единиц отдаёт 404 (`l3_not_found`) — нужна политика.** 404 держится, пока не заведена
  единица (слинг 939 — 30 дней, весы 1326 — 88). Решить: оставить 404 или отдавать 200 с «нет в наличии» и
  `OutOfStock` (текст «больше не доступен» при 200 Google сочтёт soft-404). Затрагивает 53 из 470
  «мёртвых» URL аудита.
- [ ] **Таблица «URL → 301 или 404» для 470 URL группы `ru_dead_product_or_page`.** 414 слагов нет в каталоге.
  Сейчас в `redirects` около 40 записей (id 725–761) ведут удалённые товары на родительскую категорию —
  согласовать единое правило (301 только на замену той же модели).
- [ ] **Аудит целей всех 765 редиректов:** сколько ведут на 404 или на другой редирект (цели считались по
  адресам на момент загрузки и устаревают при переносах и слияниях моделей).
- [x] **Контент в БД — сделано 25.09.2026** (подробности — [reply_to_seo_agent_404_audit_2026-09-24.md](reply_to_seo_agent_404_audit_2026-09-24.md)):
  15 карточек (9 костюмов с `.htm`, 6 весов со значком «Важно!»), главная (`pages` id 1), категория колясок
  (`pages` id 22), редирект id 701. Значок загружен на прод вручную (25.09), ветка `feat/vazhno-icon` ждёт слияния, чтобы файл отслеживался git.
- [ ] **Битые декоративные картинки в описаниях (≈35 карточек и `pages` id 42, 45, 50, 67).** Каталоги `bb/img`,
  `karnaval/img`, `vesy/img` и `public/rent/images/autokresla-bases/maxi_cosi_easybase` на проде отсутствуют и
  никогда не отслеживались в git (картинки под `.gitignore`; по-видимому, не переехали на новый хостинг).
  Около 300 запросов на них за 10 дней, треть с referrer. Решить: восстановить файлы из бэкапа старого
  хостинга (vh104) или убрать теги `<img>` из описаний через API. Список карточек — в отчёте агенту, раздел 5.
- [ ] **Soft-404 на выдуманную категорию.** `/ru/<раздел>/<подраздел>/zzzz` под настоящим разделом отдаёт 200 со
  страницей «Раздел не найден.»: `MainPage::isRealPage()` считает страницу настоящей, если найден только
  раздел (воспроизводится до PR #322–#324). Нужен 404, когда категория не найдена.
- [ ] **Сиротский и недостижимый контент `pages`:** id 25 (`konverty-na-vypisku`) за редиректом, в тексте три
  относительные ссылки `../vesy/vesy.htm`, `../kolybeli.htm`, `../kacheli/kacheli.htm`; id 22
  (`detskie-kolyaski`) — страница без категории отдаёт 200 «Раздел не найден.». В списке брендов колясок
  (id 22) остались Maclaren и Quinny без живых моделей — владельцу решать, оставлять ли их.
- [ ] **Слаги, не соответствующие товару:** `novogodniy_kostume_cherny_plasch` (это «Бэтмен-1»),
  `sukenka-belarus` (это куб-конструктор). MCP API слаги не меняет; смена слага требует редиректа.
- [ ] **Категория `tyu` («К УДАЛЕНИЮ»)**: скрыта, но не разобрана (см. `db_notes.md`, п. 20). Осталось решить
  судьбу 10 опубликованных моделей (перенос в карнавальные категории, если вещи физически есть, либо
  списание единиц и архивация через `ModelArchive`) и что делать с ~50 историческими моделями и тремя
  учётными позициями.
- [ ] **Фолбэк `RedirectController::notFound` и скрытые категории.** Адрес с мусорным префиксом и слагом скрытой
  модели даёт два 301 подряд (сначала на канонический `/tyu/<slug>`, затем на `/ru/karnavalnye-kostyumy`).
  Можно отдавать итоговый адрес за один шаг (`Category::hiddenCatRedirectTarget`).
- [ ] **Категории вне `subrazdel_category`** (в меню не видны): помимо `tyu` живые товары есть у
  `tolshchinomery` (2) и `aksessuary-dla-samokatov` (1) — выяснить, намеренно ли они скрыты из меню.

## Технические улучшения (отложено)

- [ ] Перевести **остальные** позиционные `INSERT ... VALUES` по проекту на явные колонки (шире, чем редизайн заявок) — устранить gotcha №1 в db_notes.
- [ ] Заявки: напоминания/дайджест по `planned_date` (после редизайна, мелкой доработкой).
- [ ] Аналитика: исключить `z_status='spam'` из отчётов по спросу (`reports.php`, MCP) — опционально.

## В работе

- Редизайн заявок — ветка `feature/zayavki-redesign`, спека [docs/superpowers/specs/2026-06-05-zayavki-redesign-design.md](superpowers/specs/2026-06-05-zayavki-redesign-design.md).
